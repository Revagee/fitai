# Что ещё нужно добавить/доработать

## ✅ Уже добавлено:

1. ✅ База данных (Firebase Firestore)
   - Функции сохранения/загрузки планов питания
   - Функции сохранения/загрузки планов тренировок
   - Функции отслеживания прогресса
   - Dashboard интегрирован с БД

2. ✅ Многоязычность (i18n)
   - next-intl настроен
   - Переводы для EN, UK, RU созданы
   - LanguageSwitcher компонент добавлен
   - Navbar использует переводы
   - Dashboard частично использует переводы

3. ✅ Структура URL с локалями
   - Middleware настроен
   - Все страницы в `app/[locale]/`
   - Navbar использует локализованные ссылки

## 🔨 Что нужно доработать:

### 1. Переводы в компонентах

**LandingPage.tsx:**
- [ ] Добавить `useTranslations('landing')`
- [ ] Заменить все хардкодные строки на переводы
- [ ] Обновить ссылки для использования locale prefix

**Onboarding.tsx:**
- [ ] Определить STEPS внутри компонента с переводами
- [ ] Заменить все хардкодные строки на переводы
- [ ] Обновить `router.push('/dashboard')` на `router.push(`/${locale}/dashboard`)`

**ProgressPage.tsx:**
- [ ] Добавить `useTranslations('progress')`
- [ ] Заменить все хардкодные строки на переводы
- [ ] Интегрировать загрузку данных из БД (`getUserProgress`)
- [ ] Обновить ссылки для использования locale prefix

**AICoach.tsx:**
- [ ] Добавить `useTranslations('aiCoach')`
- [ ] Заменить хардкодные строки на переводы
- [ ] Добавить переводы для приветственного сообщения

**Dashboard.tsx:**
- [ ] Заменить оставшиеся хардкодные строки на переводы
- [ ] Обновить все места с текстом

### 2. Обновление ссылок

Все ссылки должны использовать locale prefix:
- [ ] `Link href="/onboarding"` → `Link href={`/${locale}/onboarding`}`
- [ ] `router.push('/dashboard')` → `router.push(`/${locale}/dashboard`)`
- [ ] Проверить все компоненты на наличие ссылок без locale

### 3. Обработка ошибок

- [ ] Добавить обработку ошибок при загрузке из БД
- [ ] Добавить сообщения об ошибках с переводами
- [ ] Добавить fallback для случаев когда БД недоступна

### 4. Состояния загрузки

- [ ] Улучшить индикаторы загрузки
- [ ] Добавить скелетоны для контента во время загрузки
- [ ] Добавить переводы для сообщений о загрузке

### 5. ProgressPage - интеграция с БД

- [ ] Использовать `getUserProgress()` для загрузки данных
- [ ] Использовать `saveUserProgress()` для сохранения данных
- [ ] Добавить форму для ввода веса/калорий
- [ ] Обновить графики с реальными данными из БД

### 6. Дополнительные функции

- [ ] PDF экспорт планов (используя jspdf)
- [ ] Уведомления/напоминания
- [ ] Экспорт прогресса в CSV/JSON
- [ ] Возможность редактирования планов

## 📝 Приоритеты:

**Высокий приоритет:**
1. Переводы во всех компонентах
2. Обновление ссылок с locale prefix
3. Интеграция ProgressPage с БД

**Средний приоритет:**
4. Обработка ошибок
5. Улучшение состояний загрузки
6. PDF экспорт

**Низкий приоритет:**
7. Дополнительные функции
8. Уведомления

## 🚀 Быстрый старт для доработки:

1. **Обновить LandingPage:**
   ```tsx
   import { useTranslations, useLocale } from 'next-intl'
   const t = useTranslations('landing')
   const locale = useLocale()
   ```

2. **Обновить ссылки:**
   ```tsx
   <Link href={`/${locale}/onboarding`}>
   ```

3. **Обновить router.push:**
   ```tsx
   router.push(`/${locale}/dashboard`)
   ```

4. **Добавить загрузку прогресса:**
   ```tsx
   import { getUserProgress } from '@/lib/database'
   const progress = await getUserProgress(user.id, 30)
   ```

